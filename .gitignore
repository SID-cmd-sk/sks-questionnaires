# ── Never commit real secrets ────────────────────────────────
# env.js  ← removed from ignore so GitHub Pages can serve it
.env
.env.local
.env.production

# ── Python artifacts ─────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.spec
build/
dist/
*.egg-info/

# ── Database (contains customer data — keep local) ───────────
database.xlsx
*.xlsx

# ── ZIP artifacts (large/generated, keep local) ─────────────────
*.zip
processed/
error/

# ── Log files ────────────────────────────────────────────────
*.log

# ── OS files ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ───────────────────────────────────────────────────
.vscode/
.idea/
*.swp
